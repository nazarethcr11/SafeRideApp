import 'package:flutter/material.dart';

class ContactsInfoScreen extends StatelessWidget {
  const ContactsInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color.fromRGBO(31, 49, 167, 1),
    );
  }
}